# FIX LOG 2026-07-12

## 226 数据语义纠错

- 修了什么：确认 OpenRouter frontend 榜单只披露少量头部模型和 `Others`，撤销 Google/OpenAI/Qwen 未单列即为 0 的错误假设。
- 修改逻辑：不再计算厂商总份额；改为展示每周真实披露的 Top 3 模型身份、Token 和占总量比例，并把长尾单独显示为未逐模型披露。
- 修改文件：`html_dashboard/build_time_series_dashboard.py`、`docs/DATA_AUDIT_2026-07-12.md`。
- 验证结果：2026-07-06 Top 3 为 Tencent 11.0%、Xiaomi 10.6%、DeepSeek 10.1%，Others 36.4%；未单列厂商不生成零值。

## 227 删除无决策价值图表

- 修了什么：删除厂商份额、集中度、免费份额、工具/图像活动、4 快照订单薄、4 快照云价、低覆盖加权成本和价格匹配覆盖率。
- 修改逻辑：只有能回答需求总量、模型领导权、固定模型牌价或短期 GPU 方向的问题才进入主页面。
- 修改文件：`html_dashboard/build_time_series_dashboard.py`、`test_suite/test_time_series_dashboard.py`。
- 验证结果：正式页面由 10 张图收敛为 5 张互不重复图；稀疏快照数据仍保留在生产库，不在 UI 中冒充趋势。

## 228 一年固定模型 Token 牌价

- 修了什么：新增 LiteLLM Git 历史回填，逐周读取同一模型目录键的 input/output 牌价。
- 修改逻辑：使用 53 个 as-of commits，保留模型尚未出现的真实缺失；不混用不同模型、不做低覆盖加权。
- 修改文件：`scripts/backfill_litellm_key_model_prices.py`、`tracker_data/backfills/litellm_key_model_prices_1y.json`、`html_dashboard/build_time_series_dashboard.py`。
- 真实样本：GPT-4o output `$10/1M`、GPT-4.1 `$8/1M`、Claude Sonnet 4 `$15/1M`、Gemini 2.5 Flash `$2.5/1M` 在覆盖期内持平；DeepSeek Chat 从 `$1.70/1M` 降至 `$0.42/1M`。
- 失败暴露方式：回填失败、Git commit 不存在或目录键缺失时直接缺行/报错，不沿用伪造值。

## 229 交互与视觉查验

- 日期筛选：结束日期改为 2026-01-12 后，Top 3、Others 数字和所有坐标轴同步刷新；GPU 因当时没有数据而显示明确空态。
- 图例：每条价格线可独立隐藏，重绘后坐标轴按可见系列计算。
- 视觉：1440px 和 390px 均无横向溢出；移动端图表高度 190px、SVG 轴标放大；图表标题、单位、日期、图例、来源入口齐全。
- 测试：3 passed，0 failed。
- 本地查验文件：`/Users/agg/Documents/New project 2/tracker_v2/html_dashboard/ai_compute_economics_monitor.html`。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`（服务已启动，代码审查组可直接访问）。

## 230 Foundry Signals GPU 历史

- 修了什么：正式页面新增 Foundry Signals 的 GPU Price Index 和 GPU Availability Index，不再只显示 10 日 GPU 聚合价格。
- 修改逻辑：价格保留 H100/H200/B200 日均价、最低价和最高价；可用率保持来源原始频率，不把月度数据插值为日度。
- 修改文件：`scripts/backfill_foundry_signals.py`、`tracker_data/backfills/foundry_signals_gpu_history.json`、`html_dashboard/build_time_series_dashboard.py`、`test_suite/test_time_series_dashboard.py`、`CONTEXT.md`。
- 真实样本：价格共 630 条，三种 GPU 各 210 日，覆盖 2025-12-14 至 2026-07-11；可用率 49 条，H100 35 个月、B200 11 个月、H200 3 个月。
- 失败暴露方式：抓取失败、`data` schema 缺失或标准化结果为空时构建直接失败；每次抓取保存原始响应、SHA-256 和 UTC 抓取时间。
- 验证结果：3 passed，0 failed；日期筛选后价格点 630 -> 123，可用率点 49 -> 6；关闭 H100 后价格路径 3 -> 2；控制台 0 error/warn；390px 页面宽度 390px。
- 数据限制：Foundry 没有公开稳定性承诺或明确的数据许可证，网站注明数据为 illustrative，供应商覆盖也曾变化；正式页面按第三方公开代理标注。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`（服务已启动，代码审查组可直接访问）。

## 231 GPU 与 OpenRouter 活跃模型价格体系

- 修了什么：完成三代 GPU 价格分面、availability 分面、GPU 代际溢价、活跃模型 output 价格层级、活跃组合 input/output 牌价和按需单模型历史。
- 修改逻辑：Foundry 使用供应商中位价而非变动样本混合平均；OpenRouter 使用最近 4 周 Token 量筛选活跃集合，不把 871 个历史模型平均对待；免费必须有 `:free` 标记，未映射与 Others 保留为 Unknown。
- 修改文件：`scripts/backfill_foundry_signals.py`、`scripts/backfill_openrouter_active_prices.py`、`html_dashboard/build_time_series_dashboard.py`、`test_suite/test_time_series_dashboard.py`、`README_v2.md`、`ARCHITECTURE.md`、`CONTEXT.md`。
- 真实样本：Foundry 630 个价格点、49 个 availability 点；OpenRouterList 871 个历史模型、345 个当前模型、515 个模型/版本别名；当前活跃集合 8 个模型。
- 失败暴露方式：来源 schema 或数量异常时抓取失败；Unknown 不补零；组合牌价显示 coverage；H200 只有 3 点时强制 point-only；供应商变化垂直标记。
- 验证结果：目标数据与 dashboard 回归 40 passed；页面 15 个 SVG、0 空图、0 console error/warn；日期筛选与价格层级图例重绘通过；390px 无横向溢出。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`（服务已启动，代码审查组可直接访问）。

## 232 删除固定模型牌价与活跃组合更替

- 修了什么：删除 `Token价格` section、固定代表模型 output/input 两图和对应导航；把 Top 3 身份热力图改为每周活跃模型组合。
- 修改逻辑：每周使用 9 个公开模型加 `Others` 的真实份额形成独立 100% 堆叠柱；不跨周连接，避免模型未披露时被误记为 0。最新周列出全部 10 个组成部分。
- 修改文件：`html_dashboard/build_time_series_dashboard.py`、`test_suite/test_time_series_dashboard.py`、`README_v2.md`、`ARCHITECTURE.md`、`CONTEXT.md`。
- 验证结果：完整 dashboard 回归 40 passed；52 周、520 个组合 segment、每周 10 项且合计 100%；旧 Token 标题 0；页面 13 个 SVG、0 空图、0 console error/warn；390px 下为 2 列且无横向溢出。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`（服务已启动，代码审查组可直接访问）。
## 233 GitHub + Vercel 零成本自动更新

- 修了什么：正式构建不再依赖本地 DuckDB；新增 JSON 数据层、CAPEX 官方刷新、每日 GitHub Actions、Vercel 静态发布配置和来源失败保留机制。
- 修改逻辑：GitHub Actions 每日刷新公开数据并运行测试，只有成功构建后的 JSON/HTML 会提交；Vercel 监听主分支并发布 `public/index.html`。
- 修改文件：`html_dashboard/build_time_series_dashboard.py`、`scripts/refresh_capex_history.py`、`scripts/refresh_and_build.py`、`.github/workflows/refresh-dashboard.yml`、`requirements-deploy.txt`、`vercel.json`、`.gitignore`、`README_v2.md`、`ARCHITECTURE.md`。
- 验证结果：OpenRouter 52 周、Foundry 630 条价格/49 条 availability、OpenRouterList 871 个模型、CAPEX 24 条；完整 dashboard 回归 40 passed。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`。
- 线上结果：私有 GitHub Actions 已真实运行成功；Vercel 已连接仓库，Git 推送触发的新生产部署验证通过；正式地址为 `https://trackerv2-git-main-angusggsimids-projects.vercel.app`。
- 遗留问题：无。

## 234 Sites 私密镜像与 Codex 自动同步

- 修了什么：建立独立 Sites 构建目录，复用正式 Dashboard HTML，完成私密发布并创建每日 Codex 同步任务。
- 修改逻辑：GitHub/Vercel 保持主生产链路；Codex 每天 09:00 验证 GitHub 当日刷新、四源 fresh 和 Dashboard 测试，只有全部通过才发布 Sites 新版本。
- 验证结果：Sites vinext build 通过；archive 包含 `dist/server/index.js`、`dist/client/dashboard.html` 和 hosting metadata；Sites version 1 私密部署成功。
- 线上地址：`https://ai-compute-economics-tracker.angusgu456396.chatgpt.site`。
- 失败暴露：数据、测试、构建或发布任一失败均不覆盖上一版 Sites，并在自动化结果中报告失败环节。
