# FIX LOG 2026-07-11

## 187-188 唯一入口与归档边界

- 修复：`tracker_v2`、生产 DuckDB 和单页 HTML 成为唯一正式入口；旧静态版、Streamlit 和 CSI 标记 archive/demo。
- 文件：根 `README.md`、`tracker_v2/README_v2.md`、`tracker_v2/ARCHITECTURE.md`、`compute-tracker/README.md`、`tracker_v2/dashboard_v2.py`。
- 验证：正式运行路径和归档边界均可从 README 直接识别。

## 189 RunPod 数据污染

- 修复：过滤 MIG；不可用 cloud tier 的价格置空；非正价格置空；隔离统计写入质量事件。
- 文件：`tracker_v2/data_sources/market_facts.py`、`tracker_v2/test_suite/test_market_facts.py`。
- 验证：analysis 层 RunPod 非正价格 0，MIG 0。

## 190 GPUMarkets 30D 显示

- 修复：前端读取正确字段 `delta30d`。
- 文件：`tracker_v2/html_dashboard/build_html_dashboard.py`。
- 验证：extract 中 10/10 fixing 记录含 30D delta。

## 191-192 Canonical 与质量状态

- 修复：新增 canonical、analysis、latest quality 三个视图；原始历史不删除。
- 文件：`tracker_v2/tracker_v2.py`、`tracker_v2/test_suite/test_data_contract.py`。
- 验证：18,135 raw -> 17,908 canonical -> 17,302 analysis；完全重复组 0；125 条质量历史 -> 23 个最新状态。

## 193 趋势资格

- 修复：GPU 主图改为同源同口径 10 日趋势；OpenRouter 排除未完成周；token 单序列至少 5 个日期；所有 dashboard 查询改读 analysis view；RunPod 订单簿只比较 on-demand/uninterruptable，不把 spot/bid 与其他来源混排。
- 文件：`tracker_v2/html_dashboard/build_html_dashboard.py`。
- 验证：GPU 30 点覆盖 3 个 GPU x 10 日；OpenRouter 最新完整周为 2026-06-29。

## 194 验证

- 测试：39 passed，0 failed；另有本机 LibreSSL/urllib3 环境 warning 1 条，不影响本次数据逻辑。
- 浏览器：桌面和 390px 移动端均为 10 张 exhibit、12 个 SVG、0 横向溢出、0 console warning/error；日期缩窄后 GPU 图点数从 30 自动刷新为 24。
- 本地查验文件：`/Users/agg/Documents/New project 2/tracker_v2/html_dashboard/ai_compute_trend_board.html`。
- 本地查验地址：`http://127.0.0.1:8766/ai_compute_trend_board.html`（服务已启动）。

## 195-200 Phase 2 可比序列层

- 新增：稳定 series、自然频率、证据等级、完整周期、chart/inflection/90D 资格、事件变化和 GPU matched-panel engine。
- 数据边界：ComputePrices 聚合趋势因 provider composition 波动，不进入 matched-panel；普通 GPU rental 缺 exact configuration，也不进入。
- 生产结果：14,413 series；13 chart eligible；0 inflection；0 90D；matched members 0。
- 事件结果：token price change 61；美国 CAPEX actual 5；官方 guidance/RPO/capacity 9；中国 CAPEX 5。
- 文件：`tracker_v2.py`、`test_suite/test_series_layer.py`、`docs/series_contract.md`、`ARCHITECTURE.md`。
- 验证：46 passed，0 failed。Phase 2 不改旧 HTML 视觉层；Phase 5 UI 将只读取新 line/event-ready views。

## 201-206 Phase 3 持续采集与回填

- 新增：GPUPerHour exact-config 日度聚合、source SLA/freshness、pipeline latest、series gap、带锁 daily runner、结构化快照 backfill、launchd 模板。
- 真实运行：market-facts 写入 16,308 行；核心来源全部 fresh；29 个质量事件保留失败暴露。
- 回填：27 个 JSON 快照、477 条 facts、3 个历史日期；exact series 237，146 个 >=2 日，94 个=3 日。
- 当前资格：exact-config chart-ready 0、matched member 0；原因是尚未达到 10 日最低历史，不用文案升级。
- 自动任务：模板 `ops/com.agg.ai-compute-tracker.phase3.daily.plist.example` 已校验，但未安装系统任务。
- 查验：`python3 scripts/run_phase3_daily.py --audit-only` 返回 SUCCESS；日志保存在 `tracker_data/phase3_runs/`。
- 验证：相关 Phase 1-3、来源、数据合同和 dashboard 查询测试 49 passed，0 failed；launchd plist lint 通过；HTML 已用最新生产数据重建。

## 207-213 Phase 4 四时钟状态机

- 新增：Supply Price、Capacity & Utilization、Demand & Unit Economics、Commitment & Monetization 四个独立状态机。
- 禁止：不读取 legacy CSI、不生成 composite score、不把季度事件插值成日频、不让 public proxy 触发 inflection。
- 当前结果：Supply Observing；Capacity Observing；Demand Trend；Commitment Observing。
- 方向读数：OpenRouter proxy tool calls 4W +27.3%、image processing +11.7%；guidance 上修公司 1、下修 0。
- 输出：`tracker_data/thesis_states/latest-thesis-state.json` 和 `.md`，同时保存 versioned history 与 transitions。
- CLI：`python3 tracker_v2.py state-report --production --db ai_compute_tracker_production.db`。
- 对抗测试：价格跌但 depth 不增、市场广度不足、proxy 历史足够、CAPEX 单季度、两家公司 guidance 下修等场景均按规则处理。
- 验证：Phase 1-4 相关测试 55 passed，0 failed；daily runner audit-only SUCCESS，状态 transition 为空。

## 214-219 Phase 5 正式单页产品

- 新增：`html_dashboard/build_monitor_artifact.py`，只读生产 `series_quality`、`canonical_observation`、`line_ready_observation`、`event_observation`、`source_freshness` 和最新四时钟状态。
- 正式交付：`html_dashboard/ai_compute_economics_monitor.html`；旧 `ai_compute_trend_board.html` 降为历史参考。
- 展项：Compute Price、Market Depth、Cloud、Model Economics、Demand、Commitment；六图互不重复，各自保留 daily/snapshot/weekly/event/quarterly 自然频率。
- 失败暴露：H100/H200/B200 当前均为 3 个有效日，对照 10 日画线门槛展示；0 个 fixed matched panel 时不画假价格曲线。
- 数据修正：OpenRouter 4W MA 各自以首个完整 4 周均值=100；模型价格只取真实 `price_change`；云实例保持 VM-hour；美国 CAPEX 只比较 2026-03-31 同期四家公司，Oracle FY2026 年度与中国厂商证据放入事件账本。
- 展示修正：所有图均有 section、频率、日期、轴、单位、图例或正负方向；移动端缩短云 SKU；底表合并为少列可换行字段；每个卡片、图和表均可打开来源查询。
- 工程修复：`set_default_db_path(None)` 现在同时清理测试遗留环境变量；CLI 未传 `--db` 时仍保留正常环境变量 fallback。
- 验证：portable artifact validation/package/verification 全通过；1440px/390px、来源弹窗、横向溢出均通过；Phase 1-5 正式链路测试 63 passed、0 failed。
- 遗留测试：完整 legacy suite 为 156 passed、6 failed；修复数据库隔离后 4 项恢复，剩余 2 项仍要求已废止的 `quality_gate=PASS / decision_state=` 固定总分语义，未为迎合旧断言恢复错误产品逻辑。
- 本地查验文件：`/Users/agg/Documents/New project 2/tracker_v2/html_dashboard/ai_compute_economics_monitor.html`。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`（交付前启动并查验）。

## 220-225 Phase 6 投资场景验收

- 纠偏：正式重立项规划只有 Phase 0-6；证券日线、事件收益研究和证券映射不属于本产品。
- 回退：删除误加的证券代码、配置、runner、LaunchAgent、报告文件、三张证券表和 4,940 条 Yahoo Finance 日线；dashboard 从未接入这些内容。
- 六问题验收：GPU matched panel、价格与深度、venue/cloud spread、模型价格与 usage、应用商业化修订、US/China CAPEX/guidance/RPO 均可在页面得到事实或明确不可判断。
- 产品补足：Commitment 新增两个商业化证据卡；26 个公开 ARR/adoption series 与 0 个真实正/负修订分开显示，重复快照不画趋势。
- 来源追溯：H100 offer depth 与 OpenRouter tool-call 样本均完成 dashboard -> query -> canonical observation -> stable series -> raw snapshot/hash 回溯。
- 交互验证：Demand legend 隐藏后 line paths `2 -> 1`，`aria-pressed=true -> false`；来源菜单/弹窗通过。
- 视觉验证：1440px 和 390px 均通过；商业化卡只位于 Commitment；移动端宽度 `390/390`，无横向溢出。
- 对抗验证：Phase 1-6 正式链路 68 passed、0 failed；覆盖零价/MIG、未完成周、重复快照、短历史、样本构成变化、depth 不确认、proxy 限制和单季度 CAPEX。
- 最终判断：产品验收 PASS；投资拐点仍为 Observing / partial evidence，不输出自动买卖建议。
- 验收报告：`docs/PHASE6_INVESTOR_ACCEPTANCE_2026-07-11.md`。
