# FIX_LOG_2026-07-08

## 本轮目标

把 `tracker_v2` 从“有图的 HTML 页面”推进成可复核的价格/成本/商业化趋势看板：先补真实数据源，再调整图表表达和 presentation，最后用浏览器验收。

## 修复 1：补入 GPUMarkets fixing 数据

- 修了什么：新增 `gpu_market_fixing` 生产轨道，接入 `https://gpumarkets.dev/data/fixings.csv`。
- 修改逻辑：保存 `price_usd_per_gpu_hr`、`delta_1d_pct`、`delta_7d_pct`、`delta_30d_pct`、`observations`、`venues_eligible`、`venues_total`；当前只作为 fixing + delta，不包装成完整历史曲线。
- 修改文件：`data_sources/market_facts.py`、`test_suite/test_market_facts.py`、`html_dashboard/build_html_dashboard.py`。
- 验证结果：dashboard extract 中 `gpuMarketFixing=10`，1 个 fixing 日期；HTML Exhibit 2 用横向点图展示 price / 30D delta。
- 遗留问题：GPUMarkets per-series CSV 当前只证明可读，未形成多日历史；后续靠每日快照沉淀。

## 修复 2：补入 Vast 和 RunPod 当前订单薄/底价

- 修了什么：新增 `vast_offer_snapshot` 与 `runpod_gpu_price_snapshot` 生产轨道。
- 修改逻辑：Vast 使用 `/api/v0/bundles/` verified/rentable/rented=false 当前 offers，按 GPU 聚合 `offer_count`、min/p25/median/p75 price、min bid、dlperf、reliability；RunPod 使用 `gpuTypes` GraphQL，保存 secure/community/spot price 和 capacity proxy。
- 修改文件：`data_sources/market_facts.py`、`test_suite/test_market_facts.py`、`html_dashboard/build_html_dashboard.py`。
- 验证结果：生产更新插入 tracks 包含 `vast_offer_snapshot`、`runpod_gpu_price_snapshot`；dashboard orderbook extract 33 行，来源分布为 GPUPerHour、RunPod、Vast.ai。
- 遗留问题：Vast host market metrics history 仍需 host API key；AWS 90 天 spot history 和 GCP spot 仍需凭证。

## 修复 3：重构 HTML 单页图表展示

- 修了什么：`AI Compute Price & Cost Trends` 改为三段十图：GPU/云实例价格，OpenRouter/Token/模型成本，CAPEX 官方说明。
- 修改逻辑：Exhibit 顺序改为 GPU 租赁趋势、GPUMarkets fixing、三源订单薄、官方云 VM-hour、OpenRouter token archive、OpenRouter activity proxy、token 输出价、模型输出成本、多模态成本、ARR/商业化信号。
- 修改文件：`html_dashboard/build_html_dashboard.py`。
- 验证结果：桌面默认状态 10 张图卡、12 个 SVG、2 个表、0 个空图；390px 移动端无横向溢出；所有图都有标题、坐标轴、图例或说明、来源 caption。
- 遗留问题：低频图仍只能诚实显示点图/快照；不会伪造连续趋势。

## 修复 4：修正 presentation 拥挤问题

- 修了什么：多线图不再默认堆叠右侧终点直标；超过 5 条线时只保留图例，减少视觉噪音。
- 修改逻辑：`renderLineChart` 在少于 3 个日期点时只画点不连线；多 series 时关闭终点直标；日期筛选和 legend/source 隐藏后仍重算坐标轴。
- 修改文件：`html_dashboard/build_html_dashboard.py`。
- 验证结果：Playwright 桌面与移动端截图保存为 `output/playwright/html_trend_dashboard_20260708_desktop_r2.png`、`output/playwright/html_trend_dashboard_20260708_mobile_r2.png`；控制台 0 error/warn。
- 遗留问题：OpenRouter token archive 和 ARR/commercialization 仍受公开数据稀疏限制。

## 修复 5：修正时间敏感测试

- 修了什么：两个 CLI 测试使用的 GPU price fixture 不再硬编码已经 stale 的 `2026-07-05T10:00:00Z`。
- 修改逻辑：仅 CLI 子进程测试使用运行时当前 UTC；其他固定 `AS_OF=2026-07-05T12:00:00Z` 的报告单元测试不变。
- 修改文件：`test_suite/test_data_quality.py`、`test_suite/test_report_quality.py`。
- 验证结果：目标相关测试组合 `53 passed`。
- 遗留问题：完整 `pytest test_suite -q` 运行 4 分 23 秒只完成 3 项后人工中断，未作为本轮验收标准。

## 本地查验地址

- HTML 文件：`/Users/agg/Documents/AI Compute Economics/html_dashboard/ai_compute_trend_board.html`
- 本轮 QA 地址：`http://127.0.0.1:8765/ai_compute_trend_board.html`
- 当前本地服务：已在 8765 端口启动，用于继续查验。
