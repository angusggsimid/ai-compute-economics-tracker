# FIX_LOG 2026-08-15

## 1. 停止并彻底清理 Sites 发布链路

- 做了什么：按用户决定停用 ChatGPT Sites 镜像，线上只保留 Vercel。删除 Codex 定时任务 `ai-compute-tracker-sites`（`~/.codex/automations/` 整目录）、删除本地 `sites_app/`（约 759MB，从未被 git 跟踪）、`.gitignore` 移除 `sites_app/`、`README.md`/`README_v2.md`/`ARCHITECTURE.md` 清除 Sites 地址与同步门描述。
- 验证结果：`scripts/`、`.github/`、`vercel.json` 无残留 Sites 引用；每日刷新链路不受影响（当日 schedule run `31857482727` 成功）。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`。

## 2. 新增 EdgeOne Pages 自动双发布

- 做了什么：GitHub Actions `refresh-dashboard.yml` 在提交推送后新增 `Deploy to EdgeOne Pages` 步骤（setup-node 22 + `npx edgeone@1.6.28 pages deploy public -n ai-compute-tracker -e production -a global --json`），API token 存入仓库 secret `EDGEONE_PAGES_API_TOKEN`。EdgeOne 项目 `ai-compute-tracker`（`makers-2skmd9vvyabl`，Upload 类型）已存在，本地首测部署 `dp6wwp87f7zg` 成功。
- EdgeOne 地址：`https://ai-compute-tracker-fpypxnc7.edgeone.cool`（公开访问，无登录保护；已回读 HTTP 200、标题 `AI Compute Economics`、generatedAt 与产物一致）。
- 修改了哪些文件：`.github/workflows/refresh-dashboard.yml`、`README.md`、`README_v2.md`、`ARCHITECTURE.md`、`.gitignore`、`AGENTS.md`、`CONTEXT.md`。
- 验证结果：本地 CLI 部署成功；workflow 推送后以 workflow_dispatch 复跑验证 EdgeOne 步骤（结果见 CONTEXT 当日条目）。
- 遗留风险：EdgeOne token 为 2026-07-19 CLI 登录自动生成的 API token，若过期需重新 `edgeone login` 并更新 secret。

## 3. EdgeOne 改为 marathon 同款 GitHub 绑定项目

- 做了什么：通过 Pages API 新建 Github 绑定项目 `ai-compute-tracker-git`（`makers-rhz4zld7xqk4`，main 分支、输出目录 `public/`、Static 无构建），手动触发部署 `dp08416c71by` 成功拉取私有仓库并上线；新地址 `https://ai-compute-tracker-git-7zxuduki.edgeone.cool`。
- 验证状态：项目状态已由 `Pending` 转 `Normal`；push 自动触发部署验证中（本提交即为测试推送）。若自动触发不生效，保留 Actions CLI 上传步骤作为发布机制。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`。
