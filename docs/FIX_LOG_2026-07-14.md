# FIX LOG 2026-07-14

## 1. OpenRouter 滚动窗口挤掉最旧完整周导致每日 refresh 失败

- 修了什么：修复 GitHub Actions 每日 refresh 在 OpenRouter 上游只返回 52 个周点（含 1 个未完成周）时，剔除未完成周后仅剩 51 个完整周，触发 `test_time_series_dashboard.py` 至少 52 周断言失败的问题。
- 根因（已观察）：
  - 2026-07-14 run `29303721784`：上游 `model-rankings-chart` 原始 52 点，窗口 `2025-07-21`…`2026-07-13`；`2026-07-13` 周未完成（结束日 2026-07-19）。
  - 生产逻辑正确剔除未完成周后只剩 51 个完整周；最旧完整周 `2025-07-14` 已被滚动窗口挤出上游响应。
  - 旧刷新逻辑用上游窗口整表覆盖本地历史，丢失已验证完整周。
- 修改逻辑：
  1. 继续只接受完整周（周起 +6 天严格早于 as_of UTC）。
  2. `load_prior_history` 读取已有 `tracker_data/backfills/openrouter_cost_index.json`：文件缺失视为首次运行空历史；文件损坏 / schema 不符硬失败，禁止静默当空缓存成功。
  3. 与上游完整周按 `date` 去重合并；同日以上游为准；仅保留 `date >= --start-date`（保留窗口下界，生产为 today−370）且仍完整的历史周。
  4. 合并后不足 `MIN_COMPLETE_WEEKS=52` 明确 `RuntimeError`；OpenRouter 接口失败仍直接失败。
  5. `sources.openrouter.history_provenance` 记录保留窗口、上游完整周数、本地独有周列表与覆盖规则。
  6. 不伪造周点、不纳入未完成周、不降低 52 周门槛。
- 修改文件：
  - `scripts/backfill_openrouter_cost_index.py`（完整周过滤、prior 校验、merge、provenance、52 周硬门槛）
  - `test_suite/test_openrouter_cost_index.py`（滚动挤出、去重、start-date 边界、损坏/schema 失败、不足 52、首次无缓存失败、幂等、合并满 52）
  - `CONTEXT.md`、`docs/FIX_LOG_2026-07-14.md`、`ARCHITECTURE.md`、`README_v2.md`
- 验证结果（本机 2026-07-15 复验）：
  - 真实上游只读仿真：raw=52（`2025-07-21`…`2026-07-13`）、complete=51、本地 prior=52、merge=52；`local_only_weeks=["2025-07-14"]`；不含 `2026-07-13`。
  - start-date 边界：`start=2025-07-21` 时不再保留 `2025-07-14`。
  - 损坏缓存：`load_prior_history` 抛 `RuntimeError`（corrupt）。
  - 测试：`python3 -m pytest test_suite/test_openrouter_cost_index.py test_suite/test_time_series_dashboard.py test_suite/test_deploy_refresh.py -q` → **33 passed**；以合并后 artifact 替换生产文件跑 `test_time_series_dashboard.py` 仍 7 passed。
  - 与 GitHub Actions 相同的真实 `scripts/refresh_and_build.py` 完整执行成功：`status=ready`、`publishable=true`；OpenRouter 52 周且 `local_only_weeks=["2025-07-14"]`，Foundry 639 条价格/49 条 availability，活跃模型历史 873 个，SEC CAPEX 24 条且失败来源 0；统一发布验证器通过。
- 全量 `test_suite/` 复验有 2 个既有无关失败：`test_cli_real_data.py::test_full_production_update_runs_collectors_validation_and_report` 与 `test_decision_engine.py::test_production_report_outputs_decision_state_and_not_primary_csi`；两者均位于未修改的 legacy production report/decision-engine 路径，与本次 OpenRouter 刷新链路无调用关系。
- Kimi 反证与加固：
  - `load_prior_history` 增加 `price_snapshot` 必填键（`sha`/`committed_at`/`commit_url`）与 `provider_shares` 行 schema 校验，避免部分损坏缓存被静默接受。
  - 补充测试：chart 点早于 `start-date` 被过滤、provider_shares 同日期 upstream 整组覆盖、缓存 price_snapshot 缺键/schema 不符硬失败。
- Codex 最终复核：本地独有周必须同时保留 provider composition，并在 `history_provenance` 中记录上一版缓存的生成时间、OpenRouter URL 与原始响应 SHA-256；缺 composition 或原始来源哈希时硬失败，避免只保留总量或失去来源追溯。
- 遗留问题：2 个 legacy production report/decision-engine 全量测试失败需在独立任务处理，不阻塞本次正式 GitHub dashboard workflow；本次刷新链路相关测试和真实数据发布门均已通过。

## 本地查验地址

`http://127.0.0.1:8767/ai_compute_economics_monitor.html`（HTTP 200）

正式入口不变：`html_dashboard/ai_compute_economics_monitor.html`。本轮为数据刷新稳健性修复；未改产品边界。
