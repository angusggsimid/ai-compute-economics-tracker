# FIX LOG 2026-07-13

## 1. 项目迁移到独立工作区

- 修了什么：把 AI Compute Economics 的代码、数据、快照、研究资料、Git 历史和 Sites 子项目迁入 `/Users/agg/Documents/AI Compute Economics`。
- 修改逻辑：以原 `tracker_v2` 为唯一正式项目根目录；旧路径保留兼容链接，不复制两套运行代码。
- 修改文件：项目目录结构、`AGENTS.md`、`README.md`、`CONTEXT.md`、`lessons.md`、项目内旧绝对路径。
- 验证结果：新目录和旧兼容路径均可读取正式页面；本地 HTTP 200；GitHub 主仓库与 Sites 子仓库历史完整。
- 遗留问题：无。

## 2. 每日同步任务迁移

- 修了什么：Codex 每日 Sites 同步任务的项目和工作目录改为 `/Users/agg/Documents/AI Compute Economics`。
- 修改逻辑：保留原任务 id、执行时间、GitHub 仓库、Vercel 项目和 Sites project id，只替换本地项目路径。
- 修改文件：`/Users/agg/.codex/automations/ai-compute-tracker-sites/automation.toml`。
- 验证结果：任务保持 ACTIVE，工作目录与项目 id 均已指向新项目。
- 遗留问题：无。

## 3. GitHub CAPEX 自动刷新依赖

- 修了什么：补齐 CAPEX 采集器经生产模块实际导入的 Python 依赖。
- 修改逻辑：GitHub Actions 仍通过统一 `requirements-deploy.txt` 安装，不使用失败后静默伪装成功。
- 修改文件：`requirements-deploy.txt`。
- 验证结果：真实刷新四个来源全部 `fresh`，CAPEX 24 条且失败来源为 0。
- 遗留问题：无。

## 4. 时间序列验收允许真实历史增长

- 修了什么：把 OpenRouter 相关测试从固定等于 52 周改为至少 52 周。
- 修改逻辑：一年是最低覆盖门槛，不是数据上限；第 53 周真实数据进入后不应导致自动刷新失败。
- 修改文件：`test_suite/test_time_series_dashboard.py`。
- 验证结果：目标回归 33 passed，新增真实周不会再触发固定长度断言。
- 遗留问题：无。

## 5. 排除 OpenRouter 未完成周

- 修了什么：刷新时剔除尚未结束的当前周，避免周一的少量 Token 被画成需求断崖下跌。
- 修改逻辑：只有周起始日加六天早于当前 UTC 日期时，才进入正式周度时间序列。
- 修改文件：`scripts/backfill_openrouter_cost_index.py`、`test_suite/test_time_series_dashboard.py`。
- 验证结果：OpenRouter 正式序列恢复为 52 个完整周，结束于 2026-07-06；浏览器中尾部假跌消失。
- 遗留问题：无。

## 本地查验地址

`http://127.0.0.1:8767/ai_compute_economics_monitor.html`

浏览器验收：13 个 SVG、32 条 CAPEX 表格行、桌面无横向溢出、控制台 0 error/warning。Vercel 固定地址在登录态下正常返回同一产品；Sites 私密镜像仍保持登录保护。

## 6. GitHub 到 Vercel 发布质量门

- 修了什么：修复 CAPEX 全部抓取失败仍被标成 `fresh`、degraded 数据仍可能进入静态发布的问题。
- 修改逻辑：日频/周频来源必须当天 fresh；季度 CAPEX 可在五家公司官方值均不超过 150 天时标记为 `current_for_frequency`，但不能冒充 fresh；任一缓存缺失或超期即 blocked。
- 修改文件：`scripts/refresh_capex_history.py`、`scripts/refresh_and_build.py`、`scripts/validate_deploy_refresh.py`、`.github/workflows/refresh-dashboard.yml`、`test_suite/test_deploy_refresh.py`。
- 验证结果：真实四源刷新通过，发布状态为 ready/publishable；相关回归 19 passed。
- 遗留问题：等待 GitHub Actions 与 Vercel 线上重跑验收。

## 7. GitHub 到 Codex 到 Sites 同步门

- 修了什么：把 Sites 自动任务从“只看四个 fresh 字符串”改为调用统一发布验证器，避免 GitHub 与 Sites 对数据是否可发布产生不同判断。
- 修改逻辑：只有当天状态、统一发布门和页面测试全部通过才同步并发布；失败时保留上一版 Sites。
- 修改文件：Codex 自动任务 `ai-compute-tracker-sites`、`README_v2.md`、`ARCHITECTURE.md`、`CONTEXT.md`。
- 验证结果：待完成自动任务实际触发、Sites 新版本和固定地址回读。
- 遗留问题：本轮结束前完成实际发布验证。
