# FIX_LOG 2026-08-23

## 0. 路线校准采纳与外部参考层接入（T1+T2）

- 背景：按 `docs/ROADMAP_ADJUSTMENT_2026-08-23.md`（用户已采纳 C1-C3/E1-E3）落地外部数据输入分层。本轮接入 T1/T2 中可立即验证的三个免费源。
- 做了什么：
  - 新增 `scripts/backfill_reference_indices.py` → `tracker_data/backfills/reference_index_history.json`：
    - Ornn OCPI 成交型 GPU 指数（H100 SXM/H200/A100 SXM4/B200/RTX 5090，public-3mo 滚动日线，首采 460 行）
    - Ornn OTPI 已实现 token 价指数（4 lab，public-1mo，每日快照累积）
    - SemiAnalysis 公开综合指数（H100/A100/B200 日线，2023-06 至今全历史 2655 行）+ H100 1Y 合约价区间（33 期）
  - 新增 `scripts/backfill_neocloud_prices.py` → `neocloud_provider_price_history.json`：adriannutiu/gpu-rental-prices CC BY 4.0 数据集逐 offer 归一化入库（首采 292 行、34 家供应商），作为 matched-panel 面板成员池。
  - 两源均为非阻塞信息源（`blocking=false`），失败暴露于 `deploy_refresh_status.json`；`validate_deploy_refresh.py` 来源集合扩至七个，信息源接受 fresh/partial/failed。
- 验证结果：本地九源链路 `status=ready` 全 fresh；新增 6 项解析/合并测试（含 SemiAnalysis 字段级过滤、当日整体替换、缓存损坏硬失败），相关测试 27 项通过；推送后 workflow_dispatch 复跑验证云端执行。
- 许可声明：Ornn 免费层与 SemiAnalysis 公开页引用需署名（已写入 sources 元数据）；adriannutiu 数据集 CC BY 4.0 署名 gpurentalprices.com（写入 attribution 字段）。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`。

## 1. 重新接入 GPU 订单簿每日快照（GPUPerHour / Vast.ai / RunPod）

- 背景：Phase 1–6 生产库的订单簿轨道（gpu_available_offer、runpod/vast 快照等）自 2026-07-12 停止积累；Supply Price 时钟的 Inflection 条件（"价格下降是否伴随订单簿深度扩大 >=10%"）依赖该层数据，且订单簿是时点观测、缺口无法回填。
- 做了什么：
  - 新增 `scripts/backfill_gpu_orderbook.py`：三源独立采集——GPUPerHour offers API（9 个 GPU 家族逐条报价，limit=100 实测全覆盖）、Vast bundles（verified+rentable+未租用 on-demand）、RunPod gpuTypes GraphQL（非 MIG 完整卡）。汇总为 (date, source, series) 粒度：offer 数、GPU 总数、P25/P50/P75、provider 数；unit 语义区分 gpuperhour/vast=`offers`、runpod=`types`（型号挂牌取最低可用按需档）。
  - 写入 `tracker_data/backfills/gpu_orderbook_history.json` 按日累积：当天旧观测整体被最新一轮替换（不留当日幽灵键），历史日期保留；缓存损坏/schema 不符硬失败拒绝覆盖。
  - 口径延续 Phase 1 规则：MIG 剔除、$0/负价剔除、RunPod 不可用档位价剔除、Vast 仅 verified+rentable+未租用。
  - 接入 `refresh_and_build.py` 作为第 5 个 job：非阻塞信息源（`blocking=false`），失败暴露在 `deploy_refresh_status.json` 但不阻塞主链路发布；`validate_deploy_refresh.py` 将其纳入来源集合校验，接受 fresh/partial/failed 三态，全失败时输出告警。未来接入页面展示或 Supply Price 硬门槛时再升级为阻塞源。
- 测试：新增 `test_suite/test_gpu_orderbook_backfill.py`（家族识别、$0/负价抑制、分位数与小样本回退、三源解析过滤、当日整体替换合并、缓存损坏硬失败）；更新 `test_deploy_refresh.py` 两处 fixture 补充 gpu_orderbook 来源。相关 21 项测试通过。
- 验证结果：本地完整链路 `refresh_and_build.py` 五源全 fresh（orderbook=partial/fresh 均不阻塞）；首日采集 24 行（GPUPerHour H100 48 offers 中位 $3.29、A100 73 offers 中位 $1.60 等），与 7 月中旬 DuckDB 读数同口径可衔接。推送后由 workflow_dispatch 复跑验证 Actions 云端执行。
- 本地查验地址：`http://127.0.0.1:8767/ai_compute_economics_monitor.html`。
