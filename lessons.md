# lessons

## 2026-07-13 进程成功不等于来源 fresh

- 错误：CAPEX 刷新脚本捕获五个 SEC 403、保留上一版数据后仍返回 0，外层只看退出码便把来源写成 `fresh`。
- 正确做法：采集结果必须同时输出“本次是否抓取成功”和“上一份数据按自然频率是否仍有效”；外层发布门读取结构化状态，不能只看进程退出码。
- 产品规则：日频/周频必须当天 fresh；季度数据可以在明确有效期内使用 last-known-good，但必须标成 `current_for_frequency`，超期立即阻止发布。

## 2026-07-12 排名未披露不能解释成份额为零

- 错误：OpenRouter frontend 图只返回少量头部模型和 `Others`，却把未单列的 Google、OpenAI、Qwen 写成 0%，造成错误的厂商份额曲线。
- 正确做法：先验证来源的披露上限和 denominator；未进入 Top N 只能标记为“未单列/未知”，不能生成零值。要计算真实厂商份额，必须使用带认证的 Top 50 daily dataset。
- 产品规则：集中度、免费份额、工具调用等指标即使算术成立，只要无法直接回答用户的投资问题，就不进入主页面；数据质量覆盖率只能留在审计层。

## 2026-07-12 截面数据不能替代趋势产品

- 用户要的是沿时间轴观察需求、价格、成本和份额迁移的宏观研究工具；“最新价格”“当前订单薄”“历史积累进度”即使真实，也不能回答趋势问题。
- Dashboard 的信息架构必须先按用户要判断的问题设计，再选择数据，不能按已有数据表逐块陈列。
- Token 总量与厂商份额应优先使用 OpenRouter 页面真实周度模型用量；工具调用、图像处理只能作为次级代理。
- 不同云 VM 规格不能直接用绝对价格同轴比较趋势；应按同 SKU 指数化，并在 tooltip 保留真实价格。
- 历史 Token 成本必须带价格匹配覆盖率门槛；低覆盖周宁可留空，也不能用不稳定样本制造连续曲线。
- 构成随时间变化时，优先用 100% 堆叠面积或堆叠柱，而不是多条份额折线；连续周度数据用面积，少量离散期间才用堆叠柱。
- 全局日期范围必须从所有图表数据集计算，不能由某一条较短序列决定，否则会静默隐藏其他数据集的最新观测。
- 4 个快照可以展示为离散点或斜率对照，但必须直接写出观测数量，不能用普通实线暗示已经形成稳定趋势。

## 2026-07-11 Freshness 必须统一 UTC

- 错误风险：生产库 `fetched_at` 保存 UTC-naive timestamp，直接与带 Asia/Shanghai 时区的 `current_timestamp` 比较会把 age 多算 8 小时。
- 正确做法：SLA 统一使用 `current_timestamp AT TIME ZONE 'UTC'`，并用真实刚抓取样本检查 age=0。
- 以后所有 stale、延迟和完整周期判断必须先统一时区，不能只看日期字符串。

## 2026-07-11 去重不能吞掉真实价格差异

- 错误风险：把同一日期、GPU、供应商的所有不同 URL 记录直接视为重复，会把 GPU 数量、实例配置或套餐差异造成的真实价格区间删掉。
- 正确做法：canonical 层只归并“经济字段和数值都完全相同”的重复观测；数值冲突继续保留，在后续 matched-panel/配置维度完整后再决定如何聚合。
- 以后 tracker 的去重验收必须同时检查“重复是否消失”和“冲突事实是否被误删”。

## 2026-07-10 数据行数和可视化通过不等于投资可用

- 错误：用 production 行数、图表数量、控制台无错误和移动端不溢出证明 tracker 已完成，却没有验证同一条曲线是否来自稳定可比的 series。
- 正确做法：先建立稳定 series_id、可比性门槛、完整周期过滤和 matched-panel 规则；只有通过门槛的数据才能连线和触发拐点。
- 以后涉及投资 tracker，验收必须同时检查数据可比性、时间覆盖、反证条件和用户是否能据此形成判断，不能把技术健康当成产品价值。

## 2026-07-12 浏览器内完成部署授权

- 问题：Vercel 私有 GitHub 仓库授权时，过早把可在登录态浏览器内完成的操作交给用户。
- 原因：只沿用 CLI 授权路径，没有及时切换到浏览器中的 Vercel 项目设置和 GitHub App 权限页面。
- 调整：以后处理 Vercel、GitHub、部署平台的登录态设置时，默认主动使用可用浏览器完成授权、仓库连接、部署设置和结果验证。只有密码、验证码、支付、CAPTCHA或平台明确要求本人确认时才请求用户介入。
## 2026-07-12 Vercel Hobby 的提交身份门槛

- 问题：GitHub 自动部署被 Vercel 标记为 Blocked，页面构建本身没有失败。
- 原因：提交作者使用 `agg@Macbook-M1-Air.local` 或 GitHub Actions bot 邮箱，Vercel 无法把作者映射到已授权的 Hobby 团队成员。
- 调整：本项目本地 Git 和 GitHub Actions 统一使用 GitHub 账户专属地址 `281128372+angusggsimid@users.noreply.github.com`；部署验收必须检查 Vercel 显示 Ready，不能只看 Git push 成功。
